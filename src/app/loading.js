// app/loading.js

export default function Loading() {
  return (
    <div className="flex items-center justify-center min-h-screen">
      {/* Replace this with your preferred spinner or animation */}
      <h2 className="text-xl font-bold animate-pulse">Loading content...</h2>
    </div>
  );
}
